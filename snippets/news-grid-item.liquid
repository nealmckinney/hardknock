<div class="news-grid-item">
  <div class="image block relative radius-15 overflow-hidden">
    {% capture alt_text %}{% render 'image-alt-fallback', image: article.image %}{% endcapture %}
    {% assign alt_text = alt_text | strip %}
    {{
      article.image
      | image_url: width: 720
      | image_tag: widths: '380, 720', loading: 'lazy', class: 'object-fit-cover', alt: alt_text
    }}
  </div>

  <div class="news-grid-item__content">
    <h2 class="h3 news-grid-item__title mb-2">{{ article.title }}</h2>
    <p class="news-grid-item__excerpt">
      {% assign excerpt = article.excerpt | default: article.content %}
      {{ excerpt | strip_html | truncate: 400 }}
    </p>
  </div>

  <a href="{{ article.url }}" class="button mt-2">Read The Article</a>
</div>
